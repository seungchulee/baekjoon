#include <stdio.h>
#include <algorithm>
#include <vector>
using namespace std;
vector<int> arr;
int check[21];
int main(void)
{
	for (int i = 0; i < 21; i++)
		arr.push_back(i + 1);
	int N;
	scanf("%d", &N);
	int idx;
	scanf("%d", &idx);
	if (idx == 1)
	{
		long long int t;
		scanf("%lld", &t);

	}
	else if (idx == 2)
	{
		int trr[21];
		for (int i = 0; i < N; i++)
			scanf("%d", &trr[i]);
		
	}
}